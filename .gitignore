# Dependencies
node_modules/

# Expo / RN workspace build + cache cruft (N+19 · M2 · packages/expo-demo).
# The RN toolchain entered the repo with @nuri/rn + @nuri/expo-demo
# (cost #1 of decision 65.7); these are generated, never committed.
.expo/
dist/
web-build/
expo-env.d.ts
.metro-health-check*
*.tsbuildinfo

# OS / editor cruft
.DS_Store

# Claude local-only settings (launch.json is shared, kept)
.claude/settings.local.json

# Note: packages/spec/build/ is intentionally COMMITTED (decision 35 — gitignore
# deferred until the output platform locks; the @nuri/rn render-smoke
# consumes it, and committing keeps the emitted RN contract diff reviewable).
vendor-out/
