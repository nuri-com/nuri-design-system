{
  "name": "@nuri/spec",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Nuri spec · the radix workspace · NO-DEPS / NO-SCRIPTS (convergence phase 4·3 · N+53): lib/ (web CSS + custom-elements · CSS is SoT) + pipeline/ (the .ts token SoTs + descriptors) + styles/ (generated token CSS) + build/ (frozen emitted contract · Guard F). The codegen that regenerates build/ + styles/ moved OUT to the root scripts/ dir; this package is now pure data its consumers import.",
  "//": "exports map LANDED + VALIDATED in M2 — @nuri/rn (contract.ts) is the spec's first importer (decision 65.8 deferred the validation to here). The token/schema factory-face subpaths map to the frozen build/*.ts (RAW TS · no dist · resolved under TS bundler/nodenext moduleResolution + Metro package-exports); the 4 descriptor subpaths (schema + composition-button · icon-avatar · topbar) now map to the AUTHORED source at pipeline/descriptors/ — the verbatim .ts build copies were dropped (Slice 3a · projection-model §4 · decision 80 · rn imports the SoT directly · byte-identical data). schema MUST share the descriptors' source declaration: the factory's `Descriptor<A>` mapped-type inference only inverts `Variants<A>` when the descriptor objects and `createNuriComponent`/contract.ts resolve ONE schema.ts — so the schema export follows the objects to pipeline/ (build/descriptors/schema.ts stays EMITTED + drift-guarded · its emitSchemaTs tokens-rewrite is untouched · the build emit retires with the tokens move in 3b). lib/ + styles/ + pages/ stay INTERNAL. The build/ + pipeline/ dir names are exports-hidden (decision 65.8 · the dist rename stays rejected). TRANSITIONAL (N+39 · decision 73 cl.2 / 74 · convergence `final`): ./resolve-map + ./property-spelling expose the box/stack axis SoT + the property-spelling registry from pipeline/ — the post-flip rn→spec DAG (the table was mis-homed in @nuri/rn). They are PURE DATA (only `import type`, no node/codegen deps · safe for the RN bundle). The codegen-vs-data home re-org is convergence phase 4; these dir-agnostic subpaths keep @nuri/rn's import path stable across that move. N+41 (the A3 carve) added ./palette-surface + ./interactive-effects + ./typography-axis (the 3 bespoke axis SoTs) so @nuri/prototype's namespace-CSS generator resolves them across the package boundary (import.meta.resolve · the prototype → spec DAG) — same TRANSITIONAL pure-data posture. N+48 (A4c) added ./dimensions + ./colours (the dimension + colour-primitive/semantic token SoTs) so @nuri/doc's Foundations doc emitter reads them across the boundary (the additive read-only posture · no consumer mutates them · build/ byte-identical).",
  "exports": {
    "./tokens": "./build/tokens.ts",
    "./token-paths": "./build/token-paths.ts",
    "./token-vars": "./build/token-vars.ts",
    "./interaction": "./build/interaction.ts",
    "./icons": "./build/icons.ts",
    "./palette": "./build/palette.ts",
    "./descriptors/schema": "./pipeline/descriptors/schema.ts",
    "./descriptors/composition-button": "./pipeline/descriptors/composition-button.ts",
    "./descriptors/icon-avatar": "./pipeline/descriptors/icon-avatar.ts",
    "./descriptors/topbar": "./pipeline/descriptors/topbar.ts",
    "./resolve-map": "./pipeline/resolve-map.ts",
    "./property-spelling": "./pipeline/property-spelling.ts",
    "./palette-surface": "./pipeline/palette-surface.ts",
    "./interactive-effects": "./pipeline/interactive-effects.ts",
    "./typography-axis": "./pipeline/typography-axis.ts",
    "./dimensions": "./pipeline/dimensions.ts",
    "./colours": "./pipeline/colours.ts"
  }
}
