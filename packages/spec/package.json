{
  "name": "@nuri/spec",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Nuri spec · the radix workspace: lib/ (web CSS + custom-elements · CSS is SoT) + pipeline/ (emitter) + build/ (frozen emitted contract · Guard F)",
  "//": "exports map LANDED + VALIDATED in M2 — @nuri/factory (contract.ts) is the spec's first importer (decision 65.8 deferred the validation to here). The 9 factory-face subpaths map to the frozen build/*.ts (RAW TS · no dist · resolved under TS bundler/nodenext moduleResolution + Metro package-exports). pipeline/ + lib/ + styles/ + pages/ stay INTERNAL — never exported. The build/ dir name is exports-hidden (decision 65.8 · the dist rename stays rejected).",
  "exports": {
    "./tokens": "./build/tokens.ts",
    "./token-paths": "./build/token-paths.ts",
    "./icons": "./build/icons.ts",
    "./palette": "./build/palette.ts",
    "./components/button": "./build/components/button.ts",
    "./descriptors/schema": "./build/descriptors/schema.ts",
    "./descriptors/composition-button": "./build/descriptors/composition-button.ts",
    "./descriptors/icon-avatar": "./build/descriptors/icon-avatar.ts",
    "./descriptors/topbar": "./build/descriptors/topbar.ts"
  },
  "scripts": {
    "build": "node pipeline/tokens-parser.js",
    "test": "node --test pipeline/*.test.js"
  },
  "devDependencies": {
    "@phosphor-icons/core": "^2.1.1",
    "@types/react": "^19.2.15",
    "@types/react-native": "^0.72.8",
    "postcss": "^8.4.49",
    "react": "^19.2.6",
    "react-native": "^0.80.3",
    "typescript": "^6.0.3"
  }
}
