/* ──────────────────────────────────────────────────────────────
 * NURI · PROTOTYPE · TS DATA LOADER BRIDGE
 *
 * Kept as the local import boundary for prototype pipeline modules, but the
 * implementation is the shared root TypeScript transform.
 * ────────────────────────────────────────────────────────────── */

export { loadTsDataFromPath, loadSpecData, transpileTsData } from '../../../../scripts/ts-data-loader.js';
