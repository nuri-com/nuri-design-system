---
title: Axes
layout: default
nav_order: 2
---

# Axes

The 5 namespace axes — **stack · box · palette · interactive · typography** — the
system's spine. A component is a composition of these disjoint namespaces (the
schema's waist · decision 65.3 §6); the descriptor names them, the platform-native
factory resolves them.

> **Generated docs coming at A4b.** This section is a placeholder. The axis doc
> emitter (the 5 namespace pages) is an independent emitter that slots into the
> same `@nuri/doc` shell + nav as the component pages — a clean follow-on slice.
> Until then, the axis reference lives in the frozen hand pages under `archive/components/`
> (palette.html · stack.html · box.html · typography.html · …).
