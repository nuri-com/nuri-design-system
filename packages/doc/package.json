{
  "name": "@nuri/doc",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Nuri doc · the SSG documentation site (just-the-docs / Jekyll → Pages). Consumer of @nuri/prototype (the build-free components it embeds at runtime via nuri-demo) and @nuri/spec (the descriptor + token DATA its doc-gen transforms → Markdown). decision 68 · package-architecture.md §3.4 · convergence phase 2 (A4) / §5.",
  "//": "doc owns the MD-GEN: pipeline/build.js renders each frozen @nuri/spec descriptor → generated/components/<source>.md, re-sourced onto @nuri/spec's DATA exports (tokens · token-vars · palette · descriptors/* · NEVER the pipeline classifier) via the strip.js loader (node 20 cannot import a .ts). The 3 generated pages re-emit byte-identical (the doc CI gate · git diff --exit-code generated/ · the moved Guard G). The Jekyll shell (_config.yml · _includes · index.md) + the staged runtime (stage.mjs copies @nuri/prototype's factory + primitives + namespace CSS + nuri-demo + @nuri/spec's token CSS → assets/nuri/) are build-free in the browser. DAG: doc → prototype → spec. The hand-written reference pages are FROZEN under archive/ (the A4b/A4c regen spec · NOT in the Jekyll build).",
  "dependencies": {
    "@nuri/spec": "*",
    "@nuri/prototype": "*"
  },
  "scripts": {
    "build": "node pipeline/build.js",
    "test": "node --test pipeline/*.test.js"
  }
}
