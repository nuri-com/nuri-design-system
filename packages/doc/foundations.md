---
title: Foundations
layout: default
nav_order: 1
---

# Foundations

The token vocabulary — **colour · dimension · typography** — the cascade's base
(L1 primitives → L2 the accent × theme matrix). The single TS source of truth lives
in `@nuri/spec`; the web CSS is generated from it.

> **Generated docs coming at A4c.** This section is a placeholder. The token doc
> emitter (the foundations family) slots into the same `@nuri/doc` shell + the
> `buildDocTokenInputs` helpers as the component pages — a clean follow-on slice
> (the spike-before-fan-out discipline · A4 ships the descriptor family first).
> Until then, the token reference lives in the frozen hand pages under `archive/foundations/`.
