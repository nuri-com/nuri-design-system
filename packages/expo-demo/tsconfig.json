{
  "//": "Extends expo/tsconfig.base → moduleResolution 'bundler', so the bare `@nuri/rn` import resolves via that package's `main` (index.ts) and transitively `@nuri/spec` via its exports map (N+19 · M2 · decision 65.8).",
  "//@ds": "`@ds` is the cross-repo import alias: here it resolves to the workspace @nuri/rn barrel; in consumers (design-system-probe · nuri-expo) the same specifier resolves to the vendored ds/nuri/index.ts — screen code is copy-portable between the repos. Barrel only, deliberately no `@ds/*`.",
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": { "@ds": ["../rn/index.ts"] }
  },
  "include": ["App.tsx", "index.ts", "src/**/*.ts", "src/**/*.tsx"],
  "exclude": ["node_modules", "dist", ".expo"]
}
