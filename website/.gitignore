# Staged BUILD OUTPUT — copied by stage.mjs from packages/spec (the SoT) before
# `jekyll build`; never committed (the generation thesis · no drift). Regenerate
# with `npm run build -w @nuri/spec && node website/stage.mjs`.
/assets/nuri/
/components/

# Jekyll build artefacts + local bundler state.
/_site/
/.jekyll-cache/
/.jekyll-metadata
/.bundle/
/vendor/

# Resolved in CI (Ruby 3), not locally (system Ruby 2.6 < Jekyll 4 floor).
Gemfile.lock

# Local hydration-proof page (assembled by the kramdown spike · not Jekyll).
/__preview__.html
