# Session N+5 · Semantic-cascade pipeline slice (F-TOKEN-1)

**Status**: closed
**Type**: main

## Outcome

Single deliverable shipped across two passes.
`build/tokens-parser.js` split into
`build/parsers/{primitive,semantic}.js` (the orchestrator
stays at `build/tokens-parser.js` and re-exports both parsers'
helpers for the test file's stable import surface). The semantic
walker reads the 6 cascade blocks of `styles/tokens-semantic.css`,
applies the cascade-ordering rules, and chases the var() chain
through the gray-default primitives into per-(accent × theme)
literals. Pass 1 landed the walker + cross-product resolution +
oracle-table tests; pass 2 closed the emit-boundary silent-drop:
the emitter was shipping 8 of 18 resolved tokens. After pass 2
all 18 declared semantic vars are exposed in `tokens.ts` and a
completeness test enforces the disjoint-union invariant.
F-TOKEN-1 retired.

## Ship list

1. ✓ **`build/tokens-parser.js`** split into
   `build/parsers/primitive.js` (existing primitive logic) +
   `build/parsers/semantic.js` (new cascade walker + var()-chain
   resolver + tokens.ts emitter). The orchestrator runs both
   slices on `npm run build`.
2. ✓ **`build/tokens.ts`** machine-generated · replaces the
   N+4 hand-rolled stand-in. Pass 1 emitted 3 accent-keyed +
   5 chrome exports; pass 2 extended to all 6 accent-keyed
   (`accentFg`, `accentSolid`, `accentSolidPressed`,
   `accentOnSolid`, `accentBgSubtle`, `accentBgSubtlePressed`) +
   12 chrome (3 bg + bgSubtle + bgInverse + 3 text + 3 border +
   focusRing). The `chrome` TS type literal is derived from
   `CHROME_TOKEN_EXPORTS` so the list stays the single point of
   truth. `buttonBase` numerics held as constants in the emitter
   with an explicit caveat in the generated header — tracked by
   F-FONT-1 for the future typography pipeline slice.
3. ✓ **Tests extended** · 6 primitive round-trip (unchanged) +
   5 cross-product (pass 1) + 1 completeness (pass 2) ·
   12 assertions total at `build/tokens-parser.test.js`:
   - oracle-table semantic cross-product (18 tokens × 2 accents ×
     2 themes, hand-derived from the cascade rules)
   - P4 bright-vs-saturated asymmetry (lilac frozen on solid /
     solid-pressed / on-solid; saturated swap on neutral;
     partial-redeclare in block 6 for `accent-bg-subtle`)
   - `selectorMatches` cascade-specificity port from
     `lib/docs/tokens.js`
   - `resolveValue` var()-chain walker
   - drop-in textual contract on the generated `tokens.ts`
   - **export completeness** (pass 2) · every cssVar from
     `collectSemanticVars` lands in exactly one of
     `ACCENT_TOKEN_EXPORTS` or `CHROME_TOKEN_EXPORTS` ·
     fires on silent drops or phantom exports
4. ✓ **`docs/migration-tests/button-matrix/index.tsx`** still
   typechecks against the generated file: `npx tsc -p
   docs/migration-tests/button-matrix/tsconfig.json` exit 0.
5. ✓ **`docs/RISKS.md` R2 status** updated to "primitives +
   semantic mitigated (all 18 tokens exported · no silent drops)
   · Style Dictionary shape + Unistyles consumption + typography
   pipeline slice pending"; R1 F-TOKEN-1 marked retired with the
   lilac-8 drift side-finding ("bright frozen" was wrong; only
   lilac-9 / lilac-10 are frozen — dark focus-ring is now
   `#6c58a3`).
6. ✓ **Closeout audit** via general-purpose sub-agent at the end
   of each pass:
   - Pass 1: 1 bug (FRICTIONS line refs) + batch drift fixes
     applied (HANDOFF / ROADMAP / RISKS / AGENTS / README /
     llms.txt / playground/README refreshed); 2 smells folded
     into HANDOFF "Open questions" (build/ source-vs-output
     split when SD lands; tokens.ts header vs buttonBase
     constants honesty).
   - Pass 2: closeout audit per the closeout-session procedure;
     findings folded in-pass; the
     `tokens.ts`-header-vs-`buttonBase` open question landed
     option (a) and is closed.

Anti-goals held: no Stack, no second component, no new decisions,
no new principles, no markup changes to the button-matrix pair.
The migration pair imports the same 8 names it did at the start
of N+5; the other 10 newly-exposed exports are reached by the
N+6+ pairs without a hand-roll.

## Frictions surfaced

- **F-TOKEN-1 retired** — `build/tokens.ts` now machine-generated;
  the migration pair's `import '../../../build/tokens'` is unchanged.
- Side finding: the hand-rolled `tokens.ts` had `lilac-8` marked
  "bright frozen" but only `lilac-9` and `lilac-10` are frozen —
  the generated file corrects dark focus-ring to `#6c58a3`.
- 2 smells flagged at pass-1 close ([roadmap/index.md](./index.md)
  "Open questions") — `build/` source/output split when SD lands;
  `tokens.ts` header honesty about `buttonBase`. The latter was
  resolved in pass 2.

## Decisions locked

(none — single deliverable session; classify-by-cascade refactor
that produced decisions 27 + 28 landed in N+5.5.)

## Files touched

- `build/parsers/primitive.js` (new — extracted)
- `build/parsers/semantic.js` (new — cascade walker)
- `build/tokens-parser.js` (now orchestrator)
- `build/tokens-parser.test.js` (extended to 12 assertions)
- `build/tokens.ts` (now machine-generated; was hand-rolled at N+4)
- `docs/RISKS.md` (R1 F-TOKEN-1 retired; R2 status update)
