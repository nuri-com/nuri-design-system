# Session N+3.5 · Pipeline thin slice + DRY hygiene

**Status**: closed
**Type**: bridge

## Outcome

Short bridge session that landed the two items deferred from N+3.
Pipeline thin slice is what N+4 (thesis validation) consumes; the
topbar DRY is hygiene that fell out cleanly at the same time. Late
in the session, Refactor B (DS / docs separation) landed —
[decision 26](../decisionlog.md#26-ds-surfaces-vs-docs-chrome-are-physically-separated--n35)
codified.

## Ship list

1. ✓ **Pipeline thin slice** · `build/tokens-parser.js` (Node,
   postcss) reads `styles/tokens-primitive.css` and emits
   `build/tokens.json` (DTCG-nested, `{$type, $value}` per leaf —
   themed scales 4-deep, alphas 3-deep without theme, status 3-deep).
   219 colour tokens. R2 mitigation.
2. ✓ **Round-trip test** · `build/tokens-parser.test.js` uses
   `node:test`. 6 assertions including value-equality between the
   CSS (regex-extracted) and `build/tokens.json`. All pass. R3
   mitigation.
3. ✓ **`package.json`** · minimal — `"type": "module"`, postcss
   dep, `build` + `test` scripts.
4. ✓ **Topbar control extraction** to `lib/docs/control/` ·
   new `NuriControl` factory (`renderSelect` + `renderIconButton`)
   + `.nuri-control` CSS. Replaces `.nuri-shell__topbar-control*`
   in shell.css and `.nuri-demo-card__control*` in demo.css. All
   6 docs pages updated to `<link>` + `<script>` the new files.
5. ✓ **AGENTS.md "Skill · Pipeline / DTCG export"** · placeholder
   replaced with concrete commands, paths, and DTCG shape example.
   (Now lives in [skills/pipeline-dtcg-export.md](../skills/pipeline-dtcg-export.md).)
6. ✓ **`docs/RISKS.md`** · R2 + R3 statuses updated (partial mitigation;
   Style Dictionary / Unistyles / semantic references still open).
7. ✓ **Closeout audit** · general-purpose sub-agent audited the
   diff per the closeout procedure. Bugs (semantic.html accent select
   broken by class deletion + ghost class refs in AGENTS / HANDOFF)
   and drift (README / llms.txt / ROADMAP / HANDOFF state-of-play)
   fixed in the same pass.
8. ✓ **Refactor B · DS / docs separation** (added late in N+3.5
   after the closeout audit surfaced the mixing concern).
   [Decision 26](../decisionlog.md#26-ds-surfaces-vs-docs-chrome-are-physically-separated--n35)
   codified. All 5 docs-chrome files moved to `lib/docs/`;
   `lib/components/` now contains only DS surfaces (button, scope).

## Decisions locked

[26](../decisionlog.md#26-ds-surfaces-vs-docs-chrome-are-physically-separated--n35).

## Files touched

- `build/tokens-parser.js` (new)
- `build/tokens-parser.test.js` (new)
- `build/tokens.json` (generated)
- `package.json` (new — Node deps)
- `lib/docs/control/control.js` (new — extracted)
- `lib/docs/control/control.css` (new — extracted)
- `styles/shell.css` (.nuri-shell__topbar-control* removed)
- `lib/docs/demo/demo.css` (.nuri-demo-card__control* removed)
- All 6 docs pages updated for new `<link>` / `<script>` tags
- Refactor B: 5 files moved from `lib/` to `lib/docs/`
