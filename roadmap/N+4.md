# Session N+4 · Thesis validation · button-matrix translation

**Status**: closed
**Type**: main

## Outcome

First end-to-end web↔RN translation pair shipped under
`docs/migration-tests/button-matrix/`. A button-only cross-product
(variant × accent × state × scope-tier, 8 instances) instead of a
faked sub-screen — faking Card/Stack/Heading just to "discover"
they're missing would have produced no new signal. The matrix
exhaustively stresses the surfaces Nuri actually ships today
(Button + Scope + tokens).

Reordered ahead of "second component page" in N+3 per
[RISKS R5](../docs/RISKS.md#r5--thesis-not-validated-end-to-end-the-meta-risk)
(thesis not validated end-to-end). The N+4 prompt pivoted mid-brief
from a faked sub-screen to a **button matrix** — faking
Card/Stack/Heading only to "discover" they're missing produced no
new signal.

## Ship list

1. ✓ **`docs/migration-tests/` directory** introduced (sibling to
   `docs/RISKS.md` that cites the pair). Under `docs/` because
   migration-test pairs are evidence for R5, not source for
   production code; not docs pages either (no shell, no NAV).
   Top-level `playground/` is NOT used for these — it stays
   reserved for the future view-composition workstream.
2. ✓ **`docs/migration-tests/button-matrix/index.html`** · web
   side · 8 instances across 4 rows (Tier 1 page accent · Tier 2
   self-scope · Tier 3 subtree-scope · disabled state). Minimal
   page-local CSS for the 390px viewport-like canvas and the
   hand-rolled row/group flex containers (F-LAYOUT-1 made visible).
3. ✓ **`docs/migration-tests/button-matrix/index.tsx`** · RN
   hand-translation · `Pressable`-based Button with `AccentContext`
   (Tier 3 analogue) and `accent` prop fallback (Tier 2).
   Render-prop `style` signature for pressed-state colour swap
   (F-PRESSED-1).
4. ✓ **`build/tokens.ts`** · hand-rolled semantic-resolved literals
   keyed by (accent, theme). Mirrors Style-Dictionary `js` output
   shape per P10. Lives at the path Style Dictionary's slice will
   emit to, so the migration pair's import stays stable across
   "hand-rolled now → machine-generated later". Decision b1 of the
   N+4 prompt: hand-roll rather than build the slice now — the
   fact that this file exists IS the F-TOKEN-1 finding.
5. ✓ **Typecheck contract** · `tsc -p tsconfig.json` exit 0 under
   `--jsx react-native --strict`. No bundler, no Expo runtime.
   Static typecheck is the deliverable.
6. ✓ **devDeps** added to `package.json`: react, react-native,
   @types/react, @types/react-native, typescript.
7. ✓ **8 frictions captured** at
   `docs/migration-tests/button-matrix/FRICTIONS.md` (scratch) · folded into
   [`docs/RISKS.md`](../docs/RISKS.md) R1 with workarounds, target
   sessions, and locations. R5 status updated to **partially
   validated** with explicit unknowns list.
8. ✓ **Empirical validation of decision 9** (component-token
   re-resolution on every scope boundary): the web matrix shows
   Tier 2 and Tier 3 both produce the neutral colour, confirming
   var() re-resolves at the scope element rather than freezing at
   `:root`.

## Frictions surfaced

8 captured at `docs/migration-tests/button-matrix/FRICTIONS.md` ·
folded into [RISKS R1](../docs/RISKS.md#r1--webrn-api-11--props-parity--behavioural-parity).
Highlights:

- **F-LAYOUT-1** · No Stack/HStack/VStack on either side · pulled
  forward from the N+6 plan (highest-friction missing surface).
- **F-TOKEN-1** · Semantic cascade not in `build/tokens.json` ·
  next pipeline slice was now load-bearing. (Retired in N+5.)
- **F-PRESSED-1 / F-FOCUS-1 / F-DISABLED-1** · Behavioural deltas
  on Button want a documentation surface — see
  [pages/implementation-guide.html](../pages/implementation-guide.html).

n=1 — no finding promotes to a locked decision yet. The
precommitment guardrail (RISKS R5 closure paragraph) holds.

## Decisions locked

(none — anti-goal respected; n=1 isn't enough to lock.)

## Files touched

- `docs/migration-tests/button-matrix/index.html` (new)
- `docs/migration-tests/button-matrix/index.tsx` (new)
- `docs/migration-tests/button-matrix/tsconfig.json` (new)
- `docs/migration-tests/button-matrix/FRICTIONS.md` (new)
- `build/tokens.ts` (new — hand-rolled at N+4)
- `package.json` (devDeps: react, react-native, @types/*, typescript)
- `docs/RISKS.md` (R1 + R5 expanded)
