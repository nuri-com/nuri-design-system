# Session N+1 · Foundation methodology

**Status**: closed
**Type**: main

## Outcome

Foundation methodology established. Docs template standardisation
landed via Button page redesign as the canonical component-docs
template (10 sections). `<nuri-demo>` web component built and
integrated. 4 widgets extracted to `shell.css`
(`.nuri-spec-card`, `.nuri-state-grid`, `.nuri-roadmap`,
`.nuri-tag` unified). Component-token selector pattern fixed
(`:root, [data-accent], [data-theme]` for Tier 2/3 scope
correctness).

## Ship list

- ✓ **Docs template standardisation** — Button page redesigned as
  the canonical component-docs template (10 sections: spec card ·
  hero demo · API · anatomy & parts · variants · states · theming ·
  token mapping · DTCG dictionary · roadmap)
- ✓ **`<nuri-demo>` web component** built and integrated (interactive
  example container with local scope, `<template>` source of truth)
- ✓ **4 widgets extracted** to `shell.css` (.nuri-spec-card,
  .nuri-state-grid, .nuri-roadmap, .nuri-tag unified)
- ✓ **Architecture fix** — `button.css` selector pattern
  `:root, [data-accent], [data-theme]` for Tier 2/3 scope correctness
- ✗ **AGENTS.md skill router** — DEFERRED to N+3 (needs foundation
  pages canonicalized first; otherwise the "Add a new foundation page"
  skill would point to a non-existent pattern)

## Decisions locked

1–14 (see [decisionlog.md](../decisionlog.md)) — the 2-layer token
architecture, pipeline strategy Pattern C, mobile-first interaction
model, semantic naming, variant naming (`solid` / `soft`), 3-tier
component model, Unistyles v3 as RN target, default
`data-accent=lilac`, component-token selector pattern, `<nuri-demo>`
API, Tag unification, typography rule, muted-by-default rule, layout
pattern for component pages.

## Files touched

- `pages/components/button.html` (new canonical docs template)
- `lib/components/button/button.css` (multi-selector pattern fix)
- `lib/docs/demo/demo.js`, `lib/docs/demo/demo.css` (new)
- `styles/shell.css` (4 extracted widgets)
