# Session N+2 · Foundation pages canonicalization

**Status**: closed
**Type**: main

## Outcome

Foundation template canonical defined (6 sections; 3 page shapes —
hub · token-driven · sample-driven — covered by ONE structure with
optional sections). Spec card with 4 DTCG-shaped rows on every
foundation page. All 5 foundation pages migrated. Typography v2
promoted globally to `shell.css`.

## Ship list

- ✓ **Foundation template canonical** defined (6 sections; 3 page
  shapes — hub · token-driven · sample-driven — covered by ONE
  structure with optional sections). See
  [skills/add-foundation.md](../skills/add-foundation.md) "Foundation
  template canonical".
- ✓ **Spec card with 4 DTCG-shaped rows** (Type · Layer · Source ·
  Status) on every foundation page. Machine-readable via
  `data-foundation` + `data-foundation-status` + `data-spec`/
  `data-type`/`data-layer`/`data-source` attrs.
- ✓ **All 5 foundation pages migrated** at N+2 close — colour overview
  · primitive · semantic · exploration · typography. The colour
  overview was subsequently deleted in N+3 ([decision 23](../decisionlog.md#23-entry-pages-eliminated--n3)),
  leaving the 4 token-/sample-driven pages. Page-local typography
  blocks removed; only single-use widgets (`.arch-demo` on the
  since-deleted colour.html, `.exploration-active-badge` on
  exploration.html) remained at N+2; `.arch-demo` died with
  colour.html in N+3.
- ✓ **3 widgets lifted to `shell.css`** · `.nuri-role-legend`,
  `.nuri-scale-list` + `.nuri-scale-card*` (renamed from
  `.nuri-type-card*` for generality — Spacing / Radii reuse later),
  `.nuri-card--link`.
- ✓ **Typography v2 promoted globally** to `shell.css` (NOT opt-in
  body class — [decision 17](../decisionlog.md#17-typography-v2-promoted-globally-to-shellcss--n2)).
  h1 22 · h2 17 semibold · h3 17 medium ·
  p / lead 15 muted · code 13 muted. `.nuri-shell__main { max-width:
  none }`. `.page-section` rhythm + 560-cap whitelist + `::before`
  divider. Button page-local `<style>` block stripped of these rules.
- ✓ **`.nuri-type-*` specificity bumped** to (0,0,2) via chained-class
  selectors so utility classes still override the new global `p`
  rule. `typography.html` link order updated: shell.css → typography.css.

## Decisions locked

15–20 (see [decisionlog.md](../decisionlog.md)) — demo toolbar
mirrors topbar exactly · foundation spec card = 4 DTCG-shaped rows ·
typography v2 promoted globally to `shell.css` · `.nuri-type-*`
utility classes use chained-class selectors · foundation template
variants in service of ONE structure · token table format unified
across every page.

## Files touched

- `pages/foundations/colour/primitive.html` (canonical reference)
- `pages/foundations/colour/semantic.html`
- `pages/foundations/colour/exploration.html`
- `pages/foundations/typography.html`
- `pages/foundations/colour.html` (overview — later deleted in N+3)
- `pages/components/button.html` (typography page-local styles stripped)
- `styles/shell.css` (typography v2 + 3 lifted widgets)
- `styles/typography.css` (chained-class selectors)
