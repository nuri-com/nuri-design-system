# Session N+19 · U3 · box → purely geometric (background → palette · fill → stack)

**Status**: shipped · gates 1–3 green · gate 4 (in-browser) BLOCKED by session permissions — operator
repro steps below · PR pending (amendments 42.1/60.1 **pending operator sign-off at PR review**).
**Type**: **disjointness cleanup** (the operator-locked principle: box, stack, palette own DISJOINT
attribute sets — **one node = box ⊕ stack ⊕ palette, zero attribute overlap** · decision 65.3 §6).
Box violated it twice: `background` (colour → palette's) and `fill` (flex-grow → stack's). Both
removed; every consumer migrated to the merged-node class layer (B1.5 §4.2 · the form B2b realized).
NOT here: `interactive` / factory / descriptor re-emit (B2c).
**Branch**: `feat/box-pure-geometry` (worktree `…/nuri-bpg` · based on `main` @ f9c475e — U1 box/stack
geometry + U2 palette engine in).
**Box keeps**: padding\* · radius · width/height/minHeight · center · as.

---

## What shipped

### 1 · The props removed
- `lib/components/box/box.js` — `background` + `fill` out of ATTRS; `BACKGROUNDS` guard and the
  fill branch dropped; header rewritten (geometry only · cites 42.1/60.1).
- `lib/components/box/box.css` — the five `data-background` rules and the `[data-fill]`
  decision-60 block removed, each replaced by a one-comment pointer to the owning namespace;
  header/API comment rewritten. `radius` / padding / sizing / center untouched.
- `docs/migration-tests/button-matrix/box.tsx` (RN mirror) — `background`/`fill` props,
  `BoxBackground` type, `BACKGROUND_PATH`, and their style lines removed; unused
  `resolveToken`/`TokenPath` imports dropped; the F-BOX-FG-1 header note rewritten — the box half
  of that friction retires with the prop (colour ownership is palette's end-to-end · engine B2b ·
  delivery B2c).
- Stale cross-references fixed: `stack.css` (~l.72 — the fill comment's `<nuri-box fill>` example →
  stack/merged-node examples + "stack is fill's only owner"), `scroll.css` (the fill-content-container
  prose → Stack-only), `scroll.tsx` header (`Box fill` → `Stack fill`, the historical R-EXPO-4
  correction note kept with its box reference marked retired). Verified: **no CSS coupling to
  `.nuri-box[data-fill]` exists outside box.css** (repo grep).

### 2 · Consumers migrated (the merged-node class layer)
The canonical multi-namespace form: class + data-attrs on ONE painting element
(`class="nuri-box nuri-palette" data-chrome="…" data-radius="…"` · B1.5 §4.2). `<nuri-*>` elements
stay single-namespace sugar.

| consumer | before | after |
|---|---|---|
| `lib/components/tabs/tabs.js` (the impl consumer) | composed `<nuri-box background="strong" radius="md" padding="xs">` | plain `div` · `class="nuri-box nuri-palette" data-radius="md" data-padding="xs" data-chrome="strong"` (surface = palette chrome · geometry = box) |
| `pages/components/tabs.html` | 4 textual/spec refs to the composition | all updated; the spec-table row's `data-reference` → `palette chrome=strong`; palette.css linked; box.js script dropped (no element left) |
| `pages/components/topbar.html` (hosts the centred-tabs demo — census gap, see below) | box.css/js "ride along (Tabs composes a nuri-box)" | palette.css linked · box.js dropped · comments updated |
| `pages/components/screen.html:140,156` | `<nuri-box background="strong|subtle" padding="md">` placeholders | `<div class="nuri-box nuri-palette" data-padding="md" data-chrome="strong|subtle">` (padding/surface values preserved) · palette.css linked |
| `pages/components/box.html` | Background demo section (chrome ladder · accent pair · surface card) + radius demos via `background="strong"` | Background section REMOVED → short pointer to the palette page; Radius section's cells are merged `nuri-box nuri-palette` nodes with `data-chrome="strong"`; `background`/`fill` API + token-map rows removed; roadmap entries flipped to `removed · N+19 U3` with the amendment citations. `accent-solid` demo → palette page as `variant="solid"`; **`accent-subtle` DROPPED entirely** (operator-locked: no palette slot · demo-only consumer · the `accent.bgSubtle` tokens stay — same held posture as their `-pressed` twin, which already had no component consumer) |
| `pages/playground/my-vault.html:143` | `<nuri-box padding-x="lg" padding-top="md" fill>` | ONE merged node `<div class="nuri-box nuri-stack" data-padding-x="lg" data-padding-top="md" data-fill="grow">` (stack base = flex column · `fill=grow` = `flex: 1 0 auto` ≡ old box.fill exactly); inner `<nuri-stack fill>` untouched |
| `pages/components/stack.html:204` (census gap) | fill description cites `<nuri-box fill>` | rewritten: stack is fill's only owner + the merged-node form |
| `docs/migration-tests/button-matrix/tabs.tsx` (census gap — the RN Tabs) | `<Box paddingX paddingY background="strong" radius="md">` | `<Box paddingX paddingY radius="md" style={{ backgroundColor: surface.bg }}>` with `surface = resolvePalette({ chrome: 'strong' }, { mode, accent })` — the same merged-node split (geometry = Box · colour = palette) on one `<View>`; only the bg channel consumed (parts resolve their own fg · F-BOX-FG-1 discipline) |
| `lib/components/tabs/tabs.css` (comments only) | "surface = composed Box" header/inline comments | merged box ⊕ palette node prose; `@layer tokens` values untouched (emit byte-identical) |

### 3 · Palette docs page (docs ARE the spec)
- NEW `pages/components/palette.html` — follows the component-page template (decision 24 · adapted
  for a class-layer component with NO element): spec card · hero (one merged node) · the
  **merged-node authoring form** (+ the one-of `data-variant`|`data-chrome` rule · accent rides the
  existing scope) · the **complete-pair rule** · live demos (chrome ladder · variant roles · accent
  scope · the migrated Tabs + screen cells as living examples) · the **dispatch table** as a
  decision-24-style machine-readable token-map (`data-prop`/`data-value`/`data-css-prop`/
  `data-dtcg`/`data-reference` rows for every contract cell, incl. the data-only `pressedBg`/
  `fgMuted` channels) · **reserved** items (`outline` · `border` role · `onSolid.muted` ·
  pressed-dispatch B2c · **element sugar P11-deferred** · `accent-subtle` retired slotless).
- `llms.txt` — palette page added under Theming (drift Guard A green); box line refreshed
  ("purely geometric").
- `lib/docs/shell.js` — sidebar: Palette under Theming; the Layout group comment updated (42 → 42.1).

### 4 · Ledger + risks
- `decisionlog.md` — **amendment 42.1** (under decision 42) + **amendment 60.1** (under decision 60),
  both appended (no existing entry edited), both marked **"Pending operator sign-off (U3 PR
  review)"**. Verbatim texts in the PR / working-agent report.
- `docs/RISKS.md` — **F-BOX-FG-1**: N+19 U3 update appended — the Box case RETIRED with the prop;
  colour ownership palette's end-to-end (engine B2b · delivery B2c); the friction name stays as the
  canonical citation for the RN no-colour-inheritance class; target → closed for Box.

## Census deviations (reality vs the coordinator's consumer census)
1. **`docs/migration-tests/button-matrix/tabs.tsx`** — the RN Tabs consumed `Box background="strong"`
   (line 134) · not in the census. Leaving it breaks gate 2 (tsc) and the zero-hits grep; migrated
   mechanically per the model (resolvePalette chrome=strong · above).
2. **`pages/components/topbar.html`** — hosts a live `<nuri-tabs>` demo, so the tabs.js change
   cascades (needs palette.css; box.js/load comments stale) · not in the census. Migrated.
3. **`pages/components/stack.html:204`** — the Stack page's own `fill` row cited `<nuri-box fill>`
   · census listed only stack.css's comment. Migrated.
4. Pre-existing docs debt NOTED, not touched (out of scope): box.html/stack.html never got B2a's
   sizing-prop / fill-enum rows (stack.html still says `fill?: boolean`); `--nuri-accent-bg-subtle`
   now has no component-CSS consumer (like its `-pressed` twin · decision-30 posture).

## Gate results
1. `npm test` → **24/24** (Guard A green with palette.html in llms.txt · **Guard E green UNTOUCHED** —
   no palette/pipeline file modified; it was never coupled to box.css and survived the removal).
   Re-run AFTER the build: 24/24 again.
2. `npx tsc -p docs/migration-tests/button-matrix` → **0 errors**.
3. `npm run build` → emit log identical to the B2b baseline (tabs: 1 decl/1 ref · palette: 7 rows ·
   all skip-emits unchanged); **byte-identity evidence**: all 16 build files byte-count-identical to
   the pristine `main` checkout at the same base commit f9c475e; `build/components/tabs.ts` (the
   brief's named risk — tabs.html spec rows do NOT feed the emit; tabs.ts derives from tabs.css
   `@layer tokens`, untouched) verified content-identical by direct read; Guard D/E equality
   assertions (re-emit == committed file) green post-build. (`git diff --exit-code build/` itself was
   not runnable — git access to the worktree is permission-denied in this session; see Blocked.)
4. In-browser — **BLOCKED** (all preview_*/eval/screenshot tools denied in this run; the preview
   server config serves the main tree). Static equivalence: the new `.nuri-palette[data-chrome]`
   rules read the SAME semantic vars (`--nuri-bg-strong` etc.) at the same layer/specificity the old
   `.nuri-box[data-background]` rules did, and B2b's in-browser gate already pinned
   `.nuri-palette[data-chrome="strong"]` computed bg+fg ≡ those vars AND box/stack geometry
   disjointness on a merged node. Operator repro: serve the worktree
   (`npx http-server …/nuri-bpg -p 8767`) → tabs.html (tablist computed bg `--nuri-bg-strong` ·
   radius 12px · padding 4px — identical to before) · screen.html · my-vault.html (scroll body fills ·
   spacers push · identical) · box.html radius cells visible · palette.html zero console errors.
5. Repo-wide grep — `data-background` **0 hits** · `.nuri-box[data-fill]` **0 hits** (all `data-fill`
   selectors are `.nuri-stack`); the only `background=` survivors are deliberate historical
   references in amendment-citing prose ("≙ the retired Box background=strong") in tabs.js/tabs.tsx
   comments + the palette page's migration notes.

## Blocked in this session (operator action)
- **In-browser gate 4** (above) — tooling denied.
- **`git add`/`commit`/`push` in the worktree** — every `git -C …/nuri-bpg` form denied by session
  permissions. The working tree at `…/nuri-bpg` holds the complete, gates-green change; commit
  message suggested in the report.

## Handoff
B2c consumes the now-clean namespaces: the descriptor re-emit speaks `palette.{variant|chrome}` +
pure box geometry; the `interactive` flag wires pressed dispatch; the factory composes the one
merged painting node tabs.js now demonstrates in production code.
