# N+48 · A4c — the Foundations doc emitter (the token family · decision 75's 3-family set CLOSED)

**Status**: SHIPPED on `feat/n48-foundations-docs` (base `main` @ `1e7155d` · N+47 / #75). **Doc-driven ·
additive · read-only.** Adds the **Foundations doc emitter** to `@nuri/doc` and generates the
token-vocabulary reference — **colour · dimension · typography** — from `@nuri/spec`'s token SoTs, filling the
**Foundations** nav placeholder. The last of decision 75's 3 generated-doc families (Components ✓ · Axes ✓ ·
**Foundations**) — and the last piece of the doc arc the "CSS guide" critique kicked off. **No engine change ·
no new decision** (§75 + §70 + §77 cover it). **[decision 2](../decisionlog.md) UNTOUCHED** (doc-gen generates
NO CSS).

## What shipped (4 generated pages NEW · 1 spec edit · the rest doc-internal)

1. **`packages/doc/pipeline/foundations-ir.js`** (NEW · the foundations-family sibling of `axis-ir.js`) — one
   builder per subject + the `FOUNDATION_DOCS` manifest (`{ source, nav, src, lead, build(d) }` · the
   `AXIS_DOCS` shape · doc owns WHICH foundations + nav order):
   - **`foundationsColourPrimitiveIr`** ← `colours.ts` — the L1 raw catalog: the active **cream** ramp +
     **lilac** (themed light/dark literal swatches) + black/white alpha (theme-invariant · one swatch).
     Theme-FIXED → literal swatches. Only cream is live (decision 31 · NOT all 7 candidate scales).
   - **`foundationsColourSemanticIr`** ← `colours.ts` + the N+22 role resolver — the L2 role matrix:
     `chrome` (theme-only) + `accent` (accent×theme), each role → its default-scope `{ref}` cascade pointer +
     a LIVE `var()` swatch + the resolved hex (`makeRoleResolver` · the slice the palette AXIS samples). The
     accent keys are bare (`solid`) → the role NAME is `accent-<key>` (palette-surface's spelling).
   - **`foundationsDimensionIr`** ← `dimensions.ts` + `tokens` — the L1 px primitives (`px-N` → `Npx`) + the
     L2 `space`/`size`/`radius` scales: each leaf → its cascade source (`{ref:12}` → the `px-12` primitive · OR
     `literal` for the off-scale `space.none`/`radius.full` sentinels) + the resolved px (from `tokens`).
   - **`foundationsTypographyIr`** ← `tokens.type` + `tokens.emphasisWeight` — the 6 size composites
     (`fontSize`/`lineHeight`/`fontWeight`/`letterSpacing`) + the orthogonal `emphasisWeight` (the de-fused
     N+45/§77 shape · NOT the old fused `${size}Em` twins). Reads the DATA; the scale stays CSS-SoT.
2. **`docs.js · emitFoundationPage` + 4 per-kind renderers** (beside `emitAxisPage`) — `renderDimension` ·
   `renderColourPrimitive` (literal swatches) · `renderColourSemantic` (live `var()` swatches) ·
   `renderTypographyScale` (the composite table + the emphasis note). Reuses
   `frontMatter`/`genHeader`/`swatch`/`titleFor`. The colour swatches are the palette-page idiom applied to
   the full primitive ramps + the role matrix.
3. **`strip.js` — the depth-aware `as const satisfies` extension.** The old line-bounded `[^,;\n]+` satisfies
   arm TRUNCATED at the comma INSIDE `Record<string, Leaf>` (`} as const satisfies Record<string, Leaf>;` →
   `}, Leaf>;`, a syntax error · the dimensions.ts / colours.ts table suffix). Replaced with `stripAsConst`,
   a depth-aware scan to the depth-0 terminator (the same `scanToDepth0` discipline the type-decl/const-anno
   strippers use). **VERIFIED**: loads both new SoTs AND all 8 currently-loaded subpaths, 0 regressions.
4. **`packages/spec/package.json` — 2 additive exports** (the ONLY spec edit · the `token-vars`/A3 precedent):
   `"./dimensions"` + `"./colours"` → `pipeline/{dimensions,colours}.ts`. No consumer uses either subpath, no
   guard enumerates the map, `packages/spec/build/` stays **byte-identical**.
5. **`build.js` · `buildFoundationDocs`** — loads the SoTs (`loadSpecData('dimensions'|'colours')`) + the
   resolved `tokens`/role-resolver (the SAME `buildDocTokenInputs`/`makeRoleResolver` the component/axis pages
   use) → emits `generated/foundations/{colour-primitive,colour-semantic,dimension,typography}.md`. `buildDocs`
   concatenates all 3 families.
6. **`foundations.md`** (the nav landing) — `has_children: true`, the placeholder blockquote retired, a
   cascade description (the generation thesis · the target-neutral → agnostic framing · the colour TS-SoT /
   type-scale CSS-SoT asymmetry). **`_config.yml`** — the `generated/foundations` → `parent: Foundations`
   default (the byte-identical-nav trick) + the stale "placeholder, populated at A4c" comments updated.
7. **`docs-drift.test.js` — a 3rd Guard-G test** (`generated/foundations/*.md` re-emits ≡ committed) + per-page
   contract pins: a dimension cascade row (`space.md` → `px-12` → `12px`) + the `radius.full` literal sentinel ·
   a colour-primitive literal swatch (cream.1 light/dark + lilac.9 frozen + an alpha overlay) · a semantic role
   (`bg-canvas` → live var + hex · the INVERSE `accent-solid` → `neutral.1.dark`) · a type composite row + the
   de-fused emphasis note.

## Page granularity — 4 pages (operator's call at the rendered checkpoint)

Rec was 3 (colour/dimension/typography); the operator chose to **split colour** (the archived hand split):
`colour-primitive.md` (the 4 ramps · 48 swatches) + `colour-semantic.md` (chrome/accent · 19 live swatches) +
`dimension.md` + `typography.md`. Each colour page is flat `## <group>` (no `###` nesting); cross-linked in the
leads. Titles via `titleFor` → "Colour Primitive" / "Colour Semantic".

## Gates (all green · LOCAL)

- **`@nuri/doc` 3/3** (Guard G · all 3 families) · `npm run build -w @nuri/doc` → the 4 foundations pages
  emit · the **8 existing pages byte-identical** (`git diff --stat generated/components generated/axes` clean) ·
  re-emit ≡ committed.
- **The 2 additive spec exports hold**: `npm run build -w @nuri/spec` → `git diff packages/spec/build/`
  **byte-identical** · **spec 50/50** · **prototype 24/24** · **rn 29/29 + 7 snapshots** · **tsc 0** (rn + expo).
- **Render gate** (visual feedback FIRST · a throwaway proof mirrors `head_custom.html` · `nuri-doc` :8768 ·
  Jekyll can't run locally): the swatches RESOLVE — literal primitives paint their hex (cream/lilac/alpha),
  the live `var()` semantics resolve at the default scope (`var(--nuri-bg-canvas)` → `#fffdf2` ·
  `--nuri-accent-solid` → `#12110b` · `--nuri-focus-ring` → `#ae91ff` = the printed hexes) AND re-theme on a
  dark toggle (bg-canvas → `#12110b`) while the literal swatches stay fixed. Console clean.

## No decision

§75 (doc owns MD-gen · spec = data + schema · the byte-identical gate) + §70 (the cascade · L1→L2) + §77
(typography de-fused) cover it; A4c is the last application of §75, not a new one.

## Deferred / next (LOG-only · accepted)

- **The doc arc is COMPLETE** — all 3 of §75's families generate from `@nuri/spec`'s SoTs (Components · Axes ·
  Foundations). The "CSS guide" critique is fully closed.
- **The type scale stays CSS-SoT** — `styles/typography.css` + `--nuri-type-*` (the typography page's
  provenance header records the asymmetry vs colour/dimension, which are TS-SoT post N+31/N+32). A phase-4
  token-value flip · [decision 2](../decisionlog.md) untouched for the type scale.
- **Next = A5 / A6** — `@nuri/playground` (the composition surface) · codegen surfaces (+ the factory harness)
  · phase-4 token residues (type-scale · `--nuri-border-*` · fonts → TS) · the descriptor long-tail.

See [`decisionlog.md` §75 / §70 / §77 / §31](../decisionlog.md) · [`roadmap/index.md`](./index.md) ·
[`roadmap/N+47.md`](./N+47.md) · [`roadmap/N+43.md`](./N+43.md).
