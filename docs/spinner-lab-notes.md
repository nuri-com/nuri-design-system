# Spinner lab notes

Production parity record. The browser lab CSS was the reconciliation authority; the shipped
realizations live in `packages/prototype/primitives/spinner.css` and
`packages/rn/primitives/spinner/`.

- Candidate 2 · staggered ring — Knobs: duration, stagger, arc inset, and ease; all four arcs use the fixed 1.5px stroke at every size. Rotation keeps its original direction, with opacity now increasing across the four phase offsets so the leading arc is darkest and the trailing arcs fade behind it. The layered trail separates cleanly at 48 and visually compresses at 18; port caveat: RN needs four animated layers with coordinated phase offsets and opacity.
- Candidate 3b · gradient outlined coin flip — The same single-circle flip uses two mirrored masked ring gradients that crossfade in phase with the `rotateY` cycle. Face-on frames balance both gradients at `0.5`; the first edge-on frame favours the left-solid gradient, and the second favours its mirror. Each gradient keeps one half fully opaque and fades the other toward `20%`, preserving a hollow 1.5px ring while reversing the perceived near/far lighting; port caveat: gradient masking, synchronized crossfades, perspective, and `rotateY` need RN-specific implementation and device checks.
- Candidate 4 · expanding ripple — Shipped with constant stroke. The web realization animates circle width/height around a fixed centre; RN uses two RNSVG circles with JS-driven animated radii. Both therefore keep a literal 1.5px stroke through the full expansion instead of scaling the stroke with the ring. The 18/24/48 web samples and the Expo web preview matched in light and dark, and the Expo development loop showed no visible stutter during inspection. The tradeoff is deliberate: RN can no longer use the native transform driver, so release-mode native smoothness under app load remains the meaningful device check. The fade stays deliberately quiet and is especially subtle at 18 in dark neutral. Verdict: keep.
- Candidate 4b · expanding quarter ripple — The three-layer `1300ms` ripple exposes only the top-right quadrant of a hidden `2em` circle anchored at the glyph's bottom-left corner. The clipped square rotates `-45deg` to match the reference and scales by `1/√2`, keeping the rotated footprint inside `1em` (24×24 at the standard size). Because the visible quadrant carries more visual mass below its geometric centre, the complete glyph receives a proportional `-0.1em` optical Y correction. The source border compensates to `2.1213px`, resolving visually to 1.5px after the fit transform. The three arcs are evenly offset by one third of the cycle (`433.333ms`). Scale and opacity use synchronized but separate tracks: scale uses a shallow `cubic-bezier(0.35, 0.2, 0.65, 0.8)` curve to soften the start and finish without recreating the previous outer-edge bunching, while opacity reaches its peak with the original ease and then uses `cubic-bezier(0.55, 0, 0.75, 0.45)` to remain stronger through more of the expansion before dissolving near the edge; port caveat: oversized geometry, clipping, nested transforms, paired animations, optical alignment, and stroke compensation need RN device checks.
