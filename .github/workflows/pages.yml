name: pages

# Deploy the GENERATED docs site to GitHub Pages (N+22 · decision 66 arc #1 ·
# north-star move 3). SEPARATE from gates.yml — it does NOT gate merges; the
# per-workspace gates (spec · prototype · rn · expo-demo · doc) stay the merge bar.
#
# Pipeline: generate the docs (npm run build -w @nuri/doc emits
# packages/doc/generated/components/*.md · re-sourced onto @nuri/spec's DATA) →
# stage the nuri runtime assets into packages/doc/ (stage.mjs · @nuri/prototype +
# @nuri/spec token CSS) → jekyll build (just-the-docs remote theme) → upload + deploy.
#
# OPERATOR PREREQ: Settings → Pages → Source = "GitHub Actions" (this repo
# currently serves the legacy .nojekyll site from the root; flipping to Actions
# mode hands deployment to this workflow). Until then this workflow is inert.
on:
  push:
    branches: [main]
  workflow_dispatch:

# Least-privilege, plus the Pages OIDC deploy token.
permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time; let an in-flight deploy finish (don't cancel).
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install (locked · workspace-aware)
        run: npm ci

      - name: Generate the docs (emits packages/doc/generated/components/*.md)
        run: npm run build -w @nuri/doc

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true
          working-directory: packages/doc

      - name: Stage the nuri runtime assets into the site
        run: node packages/doc/stage.mjs

      - name: Configure Pages (resolves the project baseurl)
        id: pages
        uses: actions/configure-pages@v5

      - name: Jekyll build (just-the-docs · correct baseurl)
        working-directory: packages/doc
        env:
          JEKYLL_ENV: production
        run: bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}"

      - name: Upload the built site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: packages/doc/_site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
